# ArgoCD user guid example(helm)
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sealed-secrets
  namespace: argocd
spec:
  project: default
  source:
    chart: sealed-secrets # chart for use
    targetRevision: 1.16.1 # chart version
    helm:
      releaseName: sealed-secrets
    repoURL: https://bitnami-labs.github.io/sealed-secrets
  destination:
    server: "https://kubernetes.default.svc"
    namespace: default
